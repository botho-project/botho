node_modules/
artifacts/
cache/
typechain-types/
# hardhat-typechain (hardhat 3) generates into types/
types/
coverage/
coverage.json
.env
# package-lock.json is COMMITTED (see #852): reproducible builds — the
# audited OpenZeppelin version must be the deployed one, and CI uses npm ci.
