[package]
name = "bt-crypto-ring-signature"
version = "7.1.0"
authors = ["Botho"]
edition = "2021"
license = "Apache-2.0"
readme = "README.md"
rust-version = { workspace = true }

[features]
alloc = [
    "serde?/alloc",
    "bt-crypto-digestible/alloc",
    "bt-util-repr-bytes/alloc",
    "curve25519-dalek/alloc",
    "ed25519-dalek/alloc",
]
serde = [
    "dep:serde",
    "curve25519-dalek/serde",
    "ed25519-dalek/serde",
    "bt-crypto-keys/serde",
]
prost = ["dep:prost", "bt-crypto-keys/prost", "bt-util-serial"]
internals = []

default = [
    "alloc",
    "serde",
    "prost",
    "bt-util-repr-bytes/default",
    "curve25519-dalek/default",
    "bt-crypto-hashes/default",
]
proptest = ["dep:proptest", "default"]

[dependencies]

curve25519-dalek = { workspace = true }

# External dependencies
displaydoc = { workspace = true }
ed25519-dalek = { workspace = true }
hex_fmt = { workspace = true, optional = true }

# Botho dependencies
bt-core-types = { workspace = true }
bt-crypto-digestible = { workspace = true, features = ["dalek", "derive"] }
bt-crypto-hashes = { workspace = true }
bt-crypto-keys = { workspace = true }
bt-util-from-random = { workspace = true }
bt-util-repr-bytes = { workspace = true }
bt-util-serial = { workspace = true, optional = true }

# Enable all default features not known to break code coverage builds
proptest = { workspace = true, features = ["default-code-coverage"], optional = true }
prost = { workspace = true, features = ["prost-derive"], optional = true }
rand_core = { workspace = true }
serde = { workspace = true, features = ["derive"], optional = true }
subtle = { workspace = true, features = ["i128"] }
zeroize = { workspace = true }

[dev-dependencies]

bt-account-keys = { workspace = true }
bt-crypto-digestible-test-utils = { workspace = true }
bt-util-serial = { workspace = true, features = ["std"] }
bt-util-test-helper = { workspace = true }
proptest = { workspace = true, features = ["default-code-coverage"] }
