# Botho Seed Node Backup Configuration
#
# Copy this file to /etc/botho/backup.env and configure:
#   sudo mkdir -p /etc/botho
#   sudo cp backup.env.example /etc/botho/backup.env
#   sudo chmod 600 /etc/botho/backup.env
#   sudo vim /etc/botho/backup.env

# S3 bucket for backup storage (REQUIRED)
BOTHO_BACKUP_BUCKET=your-backup-bucket-name

# S3 key prefix for backups (default: ledger-backups)
BOTHO_BACKUP_PREFIX=ledger-backups

# Path to ledger database (default: /var/lib/botho/ledger)
BOTHO_LEDGER_PATH=/var/lib/botho/ledger

# AWS region (default: us-east-1)
AWS_REGION=us-east-1

# Instance ID for CloudWatch metrics (default: hostname)
# BOTHO_INSTANCE_ID=i-0123456789abcdef0
