# Line-level audit comments — botho-from-the-basics.2/botho-from-the-basics.md

Severity tags: [minor] precision note; [obs] observation, no edit required; [good] verified fix worth preserving.

1. **§9.1, "at the 5-second reference pace the chain produces about 6.3 million blocks a year, so year one distributes roughly 315 million BTH … summing to roughly 611 million BTH"** — [good] Exact against WP §7 (6,307,200 blocks/yr; 315.4M; S₁ = 611,010,000). The bridge makes the 611M figure checkable mental arithmetic, and it anchors to the *monetary reference* pace §7 itself designates — the correct side of the WP's internal 5s/3s editorial wrinkle.
2. **§9.1, "twenty years in, Phase-1 coins still make up about 69% of it"** — [good] 611.0/884.9 = 69.05% (WP §7 supply projection, year-20 row). Horizon qualifier correctly repairs v1 F7.
3. **§9.3, "coins from million-BTH lineages climb past 5× toward the 6× ceiling"** — [good] Matches WP §5's ≈5.2×–6.0× band at ≳1M BTH; the v1 "saturate near 6×" overshoot is gone.
4. **§9.4, demurrage anchor sentence ("on the order of 2% … roughly half that … Factor-1 coins are exempt")** — [good] Verified against WP §10's operating point and the implemented affine law (`cluster-tax/src/demurrage.rs`); factor-3.5 is *exactly* half, so "roughly" is safely conservative. No formula imported — the cross-reference discipline holds.
5. **§9.4, "the time held"** — [obs] Under ring signatures the protocol prices elapsed time as the value-weighted centroid of the ring members' public creation heights, a proxy for the real coin's age. The primer's phrasing is the right teaching simplification; noting for completeness only.
6. **Capstone step 2, "1 pico-BTH per byte … × ~5,000 bytes × factor 1 × a small multi-output penalty (4, for the two outputs) = 20,000 pico-BTH — 20 nano-BTH"** — [good] Arithmetic and units exact; the formula now reproduces the stated figure (v1 F3 repaired). [minor → N1] The ~5,000-B size rides the Parameter appendix's "<5 KB for 2-in-2-out"; WP §5's byte table would put 2-in-2-out nearer 5.9 KB and §7's own instance uses 4,000 B → 16 nano. Same magnitude in all three; the inconsistency belongs to the whitepaper, not the primer.
7. **Capstone summary, "two inputs, each hidden among twenty candidate coins"** — [good] States the per-ring guarantee exactly (v1 F4 repaired).
8. **Capstone summary, "a fee that reveals the transaction's size and the *lineage class* … (and, for wealthier lineages, how long they sat idle), but never who and never how much"** — [minor → N2] For factor >1 the demurrage component of the public fee scales with value moved × idle time, so strictly the fee constrains a product involving the amount (WP §9 prices "Fee amount" leakage at 2–4 bits, residual ~1). Lossy-but-true as written; an optional half-clause hedge would be tighter.
9. **§8, "odds proportional to the computation it contributes, with no structural edge for specialized hardware"** — [good] Exactly WP §7's linear-reward-scaling + ASIC-resistance claims; the v1 per-watt assertion (F5) is gone.
10. **§9.6(3), "costs the attacker roughly a fifth of its position over five years while redistribution holds essentially unchanged — it does not degrade"** — [good] Matches WP §10 verbatim (~19%; +0.078 honest vs +0.076 gamed; "does not degrade"). v1 F6 repaired without importing the table.
11. **§6, ML-DSA-65 minting treatment (unchanged from v1)** — [obs] Still the declared oracle's account (WP §4); the WP↔code divergence remains an operator-side reconciliation item (carried O1). No primer edit required.
