# Scoring — botho-bridge-spec.1 (spec-review, prose/structure/consistency reviewer)

Rubric `anvil-spec-v1`, /44, advance ≥39. Integer scores, weight = per-dim max.

| # | Dimension | Weight | Score | Justification |
|---|-----------|--------|-------|---------------|
| 1 | Normative correctness | 7 | 6 | Live claims are precisely stated and target-state mechanisms are explicitly marked and registered: import tagging carries "It is \textbf{target-state}, tracked in the register (\S\ref{sec:bridge:status})" (§Peg) and the divergence note names the exact live path — "The production unwrap path (\texttt{bridge/service/src/bth\_scan.rs}) emits a recipient output with \emph{empty} cluster tags---i.e.\ factor-1" (§Implementation Status). One deduction: the base-layer privacy claim "amounts are (on the confidential-amounts roadmap) hidden in Pedersen commitments" (§Privacy at the Boundary) reads present-tense-adjacent for a property the live chain does not yet exhibit, with no register row (see comments). Code↔spec factual adjudication is the parallel spec-audit's job; this dim scored by prose judgment. |
| 2 | Internal consistency | 6 | 6 | Mechanical half: the step-3b `check_constant_consistency_multi` gate returned `found=true`, 7 declarations (5 distinct names; `import_epoch_blocks`/`import_factor_floor` each re-declared with identical value+unit), `violations: []`, `passed=true` — no value-mismatch. Judgment half: the peg invariant "\eqref{eq:bridge:peg}" is stated once and reused by reference in §Security ("Peg solvency --- \eqref{eq:bridge:peg} holds within tolerance"); the factor-1/zero-demurrage predicate is stated consistently across §Peg, §Privacy, and §Security. No unmarked prose-level drift found. |
| 3 | Claim precision | 6 | 5 | Normative claims are falsifiable and anchored to code symbols (e.g. "a repeated or equivocating signer counts once toward $t$ (verified in \texttt{release/bth.rs::validate\_release\_attestation})" — §Threshold authorization). Deduction: no RFC-2119 discipline — the spec uses lowercase prose obligations throughout ("Every privileged cross-chain action must therefore be authorized" — §Custody; "Wallet UX must warn" — §Privacy) with no conformance-keywords clause distinguishing MUST from should/may, which the rubric dim-3 row explicitly calls for. |
| 4 | Completeness | 5 | 5 | Every component the section normatively governs is covered — custody/threshold, per-chain schemes, attestation domains, exactly-once, three-Safe split, peg, privacy leakage, import tagging, five security invariants, and the status register. No dangling "TBD" in a load-bearing predicate: "no instance of an undefined-but-referenced load-bearing entity found" — the domain-tag table, the epoch equations, and the register rows each resolve their own referents. |
| 5 | Technical accuracy | 5 | 5 | Internal logic holds and the Sybil-resistance argument is airtight: "A size-based per-unwrap factor recovers size-sensitivity" but is Sybil-able, whereas the epoch key means "intra-epoch splitting piles into the same pool and still hits the high" factor (§Import), and "Diluting requires spreading across" epochs at wall-clock cost — a satisfiable, dimensionally-coherent mechanism. The decay rule "sitting idle never normalizes imported wealth" (§Import) is consistent with the circulation-only blending elsewhere in the same subsection. |
| 6 | Structure & navigation | 4 | 2 | CAPPED at 2 by the step-4c finding (three referenced exhibits do not exist — see comments). Absent the cap the prose wayfinding is strong: numbered subsections, a labelled status register, and "see \S" cross-refs that resolve ("It builds on the transaction model (\S\ref{sec:transactions})" — §opening). The cap reflects that a reader currently meets three broken image references, not the diagrams the normative flow depends on. |
| 7 | Cross-reference & versioning discipline | 4 | 2 | CAPPED at 2 by the step-4c missing-exhibit finding. Independently deducting even absent the cap: (a) the Figure 2 caption cites "register row IMP-1" (§Import figure) but the status register has no row IDs — a dangling reference; (b) the register table uses "\addlinespace" 5× yet that macro is undefined in the whitepaper `preamble.tex` (booktabs is not loaded; \toprule/\midrule/\bottomrule are \hline redefinitions), so the section is not cleanly drop-in `\input`-able; (c) no revision-history/change-marker block. Internal `\ref{fig:...}` and external `\S\ref{sec:...}` targets all resolve against the whitepaper. |
| 8 | Prose clarity | 4 | 4 | Implementer-grade sentences; a normative requirement is never ambiguous on first read. "The sanctioned way to shed cluster provenance is thus to" pay for it (§Peg) states rule-and-rationale in one clause, and "The reserve holds only" zero-demurrage coins (§Peg) delivers the peg-solvency mechanism plainly. No re-reading needed to parse what is required. |
| 9 | Rhetorical economy | 3 | 3 | Exhaustive without padding; each subsection earns its length. The Sybil-resistance rationale "time-as-cost replaces provenance-as-cost" (§Import) compresses the whole argument into a single load-bearing clause. No wandering repetition or non-normative digression found. |
| | **Total** | **44** | **38** | Below the ≥39 audit-grade threshold — block. |
