[package]
name = "bth-transaction-types"
version = "7.1.0"
authors = ["Botho"]
edition = "2021"
license = "Apache-2.0"
readme = "README.md"
rust-version = { workspace = true }

[features]
serde = ["dep:serde", "bth-crypto-keys/serde", "bth-crypto-ring-signature/serde"]
prost = ["dep:prost", "bth-crypto-keys/prost", "bth-crypto-ring-signature/prost"]
alloc = ["serde?/alloc", "bth-crypto-digestible/alloc"]

default = ["alloc", "serde", "prost"]

[dependencies]
# External dependencies
crc = { workspace = true }
displaydoc = { workspace = true }
hkdf = { workspace = true }
prost = { workspace = true, features = ["prost-derive"], optional = true }
serde = { workspace = true, features = ["derive"], optional = true }
sha2 = { workspace = true }
subtle = { workspace = true, features = ["i128"] }
zeroize = { workspace = true }

# Botho dependencies
bth-crypto-digestible = { workspace = true, features = ["dalek", "derive"] }
bth-crypto-hashes = { workspace = true }
bth-crypto-keys = { workspace = true }
bth-crypto-ring-signature = { workspace = true }

[dev-dependencies]
bth-crypto-ring-signature = { workspace = true, features = ["proptest"] }
proptest = { workspace = true, features = ["default-code-coverage"] }
