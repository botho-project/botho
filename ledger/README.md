## ledger

Historical home of the ledger crate — storage and synchronization for the
Botho blockchain. The implementation now lives inside the `botho` crate at
[`botho/src/ledger/`](../botho/src/ledger/) (LMDB-backed store and UTXO
snapshots); this directory is kept only as a pointer.
