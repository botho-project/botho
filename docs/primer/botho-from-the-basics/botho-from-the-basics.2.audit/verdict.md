# Audit verdict — botho-from-the-basics.2

**Audit: CLEAN.** Zero unresolved audit critical flags.

- **Critical flags: none.**
  - **"Contradicts cited spec"** (flag 2, ACTIVE — `spec_ref: ../sections/*.tex` resolved to all 18 whitepaper LaTeX sections under `whitepaper/sections/`): **0 contradictions found.** Every v2 insertion (the seven-item delta below) plus a sample of the load-bearing carried-forward claims was cross-checked against the resolved spec; none disagrees.
  - **"Subtly-wrong intuition"** (flag 3, always eligible): **0 false simplifications found.** Every simplification examined resolves to lossy-but-true — the reader's carried-away belief is correct in each case (per-claim table in `findings.md`).

`audit_clean: true`

## The v2 delta — each new quantitative claim, verified

1. **§9.4 demurrage anchor** — VERIFIED against spec AND code. Primer: *"at the whitepaper's representative operating point, a factor-6 lineage that sat idle for a year owes on the order of 2% of the value it finally moves — a mid-scale factor-3.5 lineage roughly half that … Factor-1 coins are exempt."* WP §10 defines the operating point verbatim ("applies 2\%/yr demurrage at factor 6", `10-economics.tex:286`) and §7 states the exemption ("Demurrage is exempt for factor-1 coins", `07-monetary.tex:254`). The implemented law (`cluster-tax/src/demurrage.rs`: `charge = value × rate × (factor−1)/(max_factor−1) × elapsed/blocks_per_year`, `rate_bps = 200` at max factor) gives exactly 2% at factor 6/one year, **exactly half** at factor 3.5 ((3.5−1)/(6−1) = 0.5 — "roughly half" is safely true), and exactly zero at factor 1. "It keeps accruing for as long as the coins sit idle" matches the code's churn-invariance property.
2. **§9.1 arithmetic bridge** — VERIFIED. ~6.3M blocks/year: WP §7 halving period is 6,307,200 blocks ≈ 1 year **at the 5 s reference** (`07-monetary.tex:492`, tail table `:214`). Year one: 50 × 6,307,200 = 315.36M ↔ supply table year 1 = 315.4M (`:99`). Halving yearly (rewards 50/25/12.5/6.25/3.125, `:110–111`); geometric sum S₁ = 611,010,000 (`:49`) ↔ "roughly 611 million." **The 5-second-vs-3-second question resolved in the primer's favor**: §7 — the section governing all emission math — declares "The 5-second level is the *monetary reference*: all emission…" (`:165`) with 3 s as the consensus floor and 40 s the idle ceiling (`:497–499`); the primer's §8 teaches exactly that triple and §9.1 anchors to the 5 s reference. §10's constants table "Min block time 5s" (`10-economics.tex:530`) remains the known WP-internal editorial item (v1 F10, carried) — a spec defect, not a primer defect.
3. **Capstone fee calculation** — VERIFIED, arithmetic and units exact. 1 pico-BTH/byte (congestion floor, `07-monetary.tex:232`) × ~5,000 B × factor 1 × output-penalty min(2,10)² = 4 (`:387–391`) = 20,000 pico-BTH = **20 nano-BTH** (1 nano-BTH = 10³ pico-BTH; 1 BTH = 10¹² pico, `:235`). The formula instance matches the spec's enforced minimum `f_min = b_dyn × size × cluster_factor + d(t⃗,v)` (`:244–248`) with d = 0 at factor 1. One observation (N1, minor): the ~5,000 B size assumption follows the Parameter appendix ("<5 KB for 2-in-2-out", `appendix-parameters.tex:45`), but §5's byte table (~4,552 B for 1-in-2-out, `05-transactions.tex:425`) implies ~5.9 KB for 2-in-2-out, and §7's own worked instance uses 4,000 B → 16 nano. All three spec statements land in the same "tens of nano" magnitude; the WP-internal size tension is an operator editorial item.
4. **"Climb past 5× toward the 6× ceiling"** — VERIFIED. WP §5 fee-multiplier table: ≳1,000,000 BTH → ≈5.2×–6.0× (`05-transactions.tex:237`); midpoint 100k → 3.5× (`:236`, curve `:216–223`). The rephrasing adopts the v1 auditor's F8 fix exactly.
5. **"~69% at year 20"** — VERIFIED. WP §7 supply projection: year 20 = 884.9M circulating (`07-monetary.tex:105`); 611.0 / 884.9 = 69.05%. The horizon qualifier ("large majority … for the chain's first decades") correctly repairs v1 F7.
6. **Fee-leakage caveat** — VERIFIED as lossy-but-true (N2, minor note). "Reveals the transaction's size and the lineage class … (and, for wealthier lineages, how long they sat idle), but never who and never how much": size ✓ (fee ∝ size), lineage class ✓ (× cluster_factor), demurrage clock ✓ (d has the elapsed term). Two precision notes that do NOT rise to a flag: (a) for factor >1 the demurrage term is proportional to *value moved × idle time* (`demurrage.rs`), so the public fee constrains the product, not idle time alone — WP §9's own metadata table prices "Fee amount" leakage at 2–4 bits, residual ~1 (`09-security.tex:276`), and the carried-away belief ("amounts are not visible on-chain") remains correct per the spec's headline claims; (b) "how long they sat idle" is the value-weighted ring-centroid of public creation heights, a proxy for the real coin's age. Both are simplifications the spec's own §7 gloss also makes (it omits the elapsed term entirely).
7. **Rephrased v1 items** — all VERIFIED. "Two inputs, each hidden among twenty candidate coins" states the per-ring guarantee (ring size 20; fixes the v1 F4 union-of-40 overclaim). RandomX "odds proportional to the computation it contributes, with no structural edge for specialized hardware" is exactly WP §7's "No economy of scale: Linear scaling of rewards" (`07-monetary.tex:434`) + "CPU-friendly PoW: Algorithm resists ASIC development" (`:430`); the v1 per-watt overreach (F5) is gone. §9.6(3) "costs the attacker roughly a fifth of its position over five years while redistribution holds essentially unchanged — it does not degrade" matches WP §10 verbatim: "costing the attacker ∼19% of its position over five years," full-system row +0.078 ± 0.0002 honest vs +0.076 gamed, "does not degrade" (`10-economics.tex:322, 346–348, 366–368`) — the v1 F6 "marginally improves" error is repaired.

## Sampled carried-forward claims (spot-check, all verified)

CLSAG ~700 B (704, `05-transactions.tex:425` table); ML-KEM-768 ciphertext 1,088 B (`03-preliminaries.tex:145`, `04-cryptography.tex:91`); ML-DSA-65 signature ~3.3 KB (3,309 B, `03-preliminaries.tex`); PQ ring signature ~35 KB/input ≈ 50× a 704-B CLSAG, several-input payment >100 KB (`05-transactions.tex:430–431`); 20% burn / 80% pool, 4 UTXOs/block, payout capped at one block reward with carryover (`07-monetary.tex:270–291`); emission-to-lottery ramp 10 pp/halving to the 50% cap (`:281, :502`); lottery seed from the committed previous block hash + grinding cost ≈ R(h) exceeds the redirectable payout (`:328–346`, `09-security.tex:729–739`); inverse-factor tilt 6:1 per BTH (`10-economics.tex` E[income] formula); dust floor 10⁶ pico = one millionth of a BTH, 720-block maturity (`07-monetary.tex:326–327`); tag decay ×0.95 per qualifying event, 720-block age gate ≈ 1 hour at 5 s (`05-transactions.tex:159–180`); decoy age-realism + ≥70% tag cosine similarity (`:435–443`); max-ring-factor fee rule (`:251`); log-domain sigmoid factor curve, W_mid = 100k (`:216–223`).

## Top audit priorities for the reviser / operator

No blocking items. In descending order:

1. **Carried operator items (unchanged from v1, no primer edit required)**: F2 — WP §4 "Minting Signatures" vs live-code divergence on ML-DSA-65's role (primer correctly follows the declared oracle; reconciliation issue is operator-side); F10 — WP-internal inconsistencies (§10 "Min block time 5s" vs §7's 3 s floor; §2 CLSAG byte figure), now joined by **N1**: the 2-in-2-out size tension (appendix "<5 KB" vs §5's byte table implying ~5.9 KB vs §7's 4,000-B worked instance).
2. **N2 (minor, optional polish)**: the capstone's absolute "never how much" could carry a half-clause hedge for factor >1 spends (the demurrage term scales with value moved as well as idle time), mirroring WP §9's 2–4-bit fee-amount leakage row. Lossy-but-true as written; not a flag.
