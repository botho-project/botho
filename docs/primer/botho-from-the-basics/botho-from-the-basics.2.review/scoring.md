# Scoring — botho-from-the-basics.2 (anvil-primer-v1, /44)

Scored on v2's own merits (no reviser credit). Prior sibling `botho-from-the-basics.1.review/` consulted only to track whether prior issues recur.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Pedagogical scaffolding / learnability | 7 | 7 | Top-to-bottom newcomer walk found no blocking un-taught concept. The two v1 forward references are gone: §4 now leans on the crowd-size contrast alone ("not in the whole universe of outputs, as Zcash's shielded pool allows" — §4), and §8's mining machinery is glossed at point of use ("a throwaway counter they vary until the whole header's hash" — §8, pointing back to §2's hash unpredictability); §7 binds the acronym at first flow use ("the family of agreement algorithms built to survive" — §7). The one remaining forward reference (§4 → §9 "cluster tags", glossed inline as "a Botho-specific notion of coin ancestry" — §4) is signposted and glossed at point of use — the exemplary pattern, not a block; recaps refresh every dependency before reuse ("Now recall the *shape* of the classical stealth address from Section 3" — §6). |
| 2 | Intuition before formalism | 6 | 6 | Every mechanism still gets its plain-language why before any machinery, and the analogies remain load-bearing and correctly scoped ("combining colors is easy, un-mixing is not" — §2, confined to one-wayness; "mining is *applying to speak*" — §8 carries the issuance/consensus decoupling exactly). The v2 quantitative insertions all arrive after their intuitions (the fee arithmetic after §9's why; the demurrage number after the parking-fee framing). No instance of notation dumped without a prior intuition found. |
| 3 | Worked-example / walkthrough concreteness | 5 | 5 | Both v1 gaps are cured on the page: demurrage now has a felt size ("owes on the order of 2% of the value it finally moves" — §9.4, with the factor-3.5 half-rate and factor-1 exemption bracketing it), and the 611M figure is now reproducible mental arithmetic ("the chain produces about 6.3 million blocks a year, so year one" distributes 315M, halving to ~611M — §9.1; 50 × 6,307,200 × 1.9375 = 611,010,000 exactly, matching WP §7). The capstone fee is now a complete traceable calculation ("a small multi-output penalty (4, for the two outputs) = 20,000 pico-BTH" — §10 step 2). |
| 4 | Technical accuracy | 5 | 5 | Reviewer judgment side: every v2 number was re-verified against the resolved spec — "2%/yr demurrage at factor 6" (WP §10:286) anchors the demurrage figure; "mid-scale factor-3.5 lineage roughly half that" (§9.4) is robust under both the implemented affine reading (0.50) and a naive proportional reading (0.58); "holds essentially unchanged — it does not degrade" (§9.6) matches WP §10's +0.078 honest / +0.076 gamed full-system row; the 69%-at-year-20 horizon matches 611.0M/884.9M. Simplifications stay lossy-but-true throughout. |
| 5 | Spec cross-reference discipline | 5 | 5 | The three quantitative insertions stay teach-then-point: the demurrage anchor is one hedged number with the formula explicitly declined ("The exact curve is a log-domain sigmoid — see the whitepaper" — §9.3 remains the model move); the blocks-per-year constant is quoted in prose, not a table; the capstone fee is a single worked instance of the primer's own 2-in-2-out example — distinct from WP §7's own equation-environment derivation (which uses 4,000 bytes → 16 nano-BTH), so nothing is reproduced. Every section still closes with a teach-then-point blockquote whose named subsections exist in the resolved spec (verified in v1; unchanged in v2). Duplication sweep clean. |
| 6 | Audience calibration | 4 | 3 | The four v1 acronym gaps are all cured at point of use ("the transaction-reordering games known as miner-extractable value (MEV)" — §9.5 is the pattern). Deduction for two residual unexpanded acronyms: "RandomX is deliberately hostile to ASICs" (§8) never expands ASIC — the surrounding contrast with "general-purpose CPU features" carries most of the meaning, but the acronym itself gets none of the treatment BFT/MEV/nonce received — and the §11 map table row "Monero, MobileCoin, Zcash, TEE approaches" (§11) drops TEE with no gloss anywhere in the primer. |
| 7 | Structure & navigation | 4 | 4 | The wayfinding apparatus is intact and un-sanded: the problems-first frame still anchors navigation ("each later section will point back at them" — §1), section intros still announce their place in the novel-to-Botho arc, the capstone touches every mechanism in order, and §11 still delivers the synthesis pass plus the by-question map table. A reader always knows where they are and why. |
| 8 | Prose clarity | 4 | 4 | The v2 insertions land on first pass despite carrying arithmetic — the capstone fee line reads as a sentence, and the closing triad still snaps ("private to observers, honest to validators" — §10). No instance of a sentence requiring re-reading to parse found. |
| 9 | Rhetorical economy | 4 | 4 | +230 words over v1, all load-bearing (an order-of-magnitude anchor, an arithmetic bridge, a complete fee instance, and hedge-tightening phrases like "the point is pressure, not punishment" — §9.4 that do argumentative work). No instance of padding or non-load-bearing digression found. |
| | **Total** | **44** | **43** | Advance threshold ≥35. |
