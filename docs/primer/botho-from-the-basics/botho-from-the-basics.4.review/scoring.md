# Scoring — botho-from-the-basics.4 (anvil-primer-v1, /44)

Scored on v4's own merits. The v3→v4 delta was verified mechanically: a `diff` of the two bodies shows exactly **six body hunks**, and a byte-compare of the ten exhibit files shows exactly **two re-authored `.mmd` sources** (fig2, fig5) with their **two re-rendered PNGs** — eight source hunks total, matching changelog items C1–C8; fig1/fig3/fig4 are byte-identical carries. Everything outside that perimeter is byte-identical to the AUDITED v3 body, so for unchanged material this review leans on the prior sibling `botho-from-the-basics.3.review/` (44/44, zero flags; cited per dimension as "v3 review") rather than re-deriving. All eight hunks, and both re-rendered PNGs, were reviewed fresh against the re-resolved (post-PR-#901) spec.

| # | Dimension | Weight | Score | Justification |
|---|---|---|---|---|
| 1 | Pedagogical scaffolding / learnability | 7 | 7 | The unchanged spine carries the v3 review's clean newcomer walk (7/7, byte-identical outside the eight hunks). The new §6 re-teach rests only on already-taught material: "The block of data a miner hashes over and over in the mining race (Section 8) *includes the miner's own public address keys*" (— §6) needs nothing beyond §2's hash one-wayness plus the same bridgeable `(Section 8)` pointer the v3 critics accepted, and the inline gloss makes the pointer optional rather than blocking. The capstone closes the loop backwards, not forwards — "the miner's identity is bound by the PoW preimage itself, per Section 6" (— §10 step 8) — and the §11 caveat uses only §5/§6 vocabulary taught long before. |
| 2 | Intuition before formalism | 6 | 6 | The re-teach is a model wrong-instinct-first beat: it voices the reader's own reflex and prices it — "and that instinct is expensive: a lattice signature plus its public key would add roughly 5.3 KB to every block, forever" (— §6) — then delivers the mechanism in pure plain language (the RandomX preimage equation stays in the spec; zero notation appears). The analogy "The work itself is the signature." (— §6) is load-bearing and correct at the property being defended: the attribution the design must make unforgeable really is enforced by the identity-bearing preimage under the difficulty target — swapping the keys destroys the exact artifact that conferred the reward, which is precisely what a signature would otherwise guarantee, and the paragraph derives that consequence rather than asserting it. |
| 3 | Worked-example / walkthrough concreteness | 5 | 5 | The v3 review's cured traces are byte-identical, and the capstone's step 8 was re-pointed in place with no loss of traceability: "no signature needed: the miner's identity is bound by the PoW preimage itself, per Section 6" (— §10 step 8) keeps the same concrete actor ("maybe a hobbyist CPU node") and the same cluster/lottery consequences. Figure 5's step-8 box now matches the prose exactly (verified visually: 'identity bound by the PoW preimage — no signature'), and the map caption is unchanged: "a miner proposes and SCP finalizes (steps 8–9)" (— §10 Figure 5 caption). |
| 4 | Technical accuracy | 5 | 5 | Judgment side: every new claim checks against the re-resolved spec. "roughly 5.3 KB saved every block versus the signature route" (— §6) = 3,309 B signature + 1,952 B public key = 5,261 B per `04-cryptography.tex:363–365`; the quantum claim "it gets only a modest generic search speedup (Grover's algorithm), nothing like an outright break" (— §6) matches the Grover-only statement at `04-cryptography.tex:359–361`; the ML-DSA-65 designated-future-family parenthetical matches `03-preliminaries.tex:149–157`. Two simplifications are lossy-but-true, not false: 'the block of data a miner hashes' for the four-field preimage (comment 2), and the reattribution chain omitting the spec's cluster-origin/stealth-output cascade — both compress in the direction of the same conclusion. |
| 5 | Spec cross-reference discipline | 5 | 5 | The duplication sweep on the new §6 is clean: the primer teaches the binding intuition and reproduces **no equation** (the RandomX preimage equation at `04-cryptography.tex:348–351` is never restated, even informally as notation), **no table** (`tab:hybrid` is rendered as a teaching diagram plus caption, not the normative four-row table), and **no derivation**. The teach-then-point blockquote survives and now points at the *renamed* spec subsection — "For the formal treatment — protocol listings, the recipient-unlinkability" (— §6 blockquote) closes with the pointer to "Minting Attribution," matching `\subsection{Minting Attribution}` in the corrected whitepaper — the exact repair C2 promised. Unchanged body's clean sweep carries over from the v3 review (5/5). |
| 6 | Audience calibration | 4 | 4 | The single status caveat is pitched exactly at the stated reader and anticipates the one place a non-specialist would catch the gap themselves: "you are looking at an implementation gap, not a different design" (— §11, after sending them to the block explorer). It is honest without undermining the teaching — §5 and §6 teach the ratified design unhedged (grep-verified: no hedge language rides in either section), and the hedging is confined to one signposted block. Grover's algorithm, the only new term v4 introduces, is glossed at point of use rather than assumed. |
| 7 | Structure & navigation | 4 | 4 | The v3 wayfinding apparatus is byte-identical, and the caveat is correctly homed: "One status note before you go." (— §11) sits at the top of the exact section that sends readers out to the repository, explorer, and literature — one signposted block, not scattered hedges, so a reader can both find it and quarantine it. The §6 subsection retitle ("Minting attribution: the work itself is the signature" — §6 heading) keeps the section's scan-ability: the heading now states the claim the subsection proves. |
| 8 | Prose clarity | 4 | 4 | The densest new sentence — the reattribution chain — parses on first read because its which-clauses cascade in causal order: "which changes the hash, which destroys the very proof of work that made the block valid" (— §6). The caveat's three facts land as one sentence each. One pre-existing terminological looseness ('permanent secrets' covering the deliberately-public minting attribution) is now slightly more visible against the corrected figure header; it is recorded as comment 1 (minor, pre-existing v3 text the prior critics passed, out of the #905 scope) and does not force a re-read in context — the surrounding sentences state the identity is public. |
| 9 | Rhetorical economy | 4 | 4 | +320 words over v3, all load-bearing: the §6 re-teach is a genuine mechanism beat, not a name swap (it must establish the binding, the reattribution consequence, the quantum argument, and the priced trade-off — "zero signature bytes per block" — §6), the parenthetical earns its place by explaining why ML-DSA still appears in §11's literature map, and the caveat is exactly ONE block as directed. No instance of padding or non-load-bearing digression found among the eight hunks; the unchanged body's economy carries over from the v3 review (4/4). |
| | **Total** | **44** | **44** | Advance threshold ≥35. |
