# Honest-overcharge surface of the #925 downgrade charge

**Issue:** [#950](https://github.com/botho-project/botho/issues/950) (informs [#925](https://github.com/botho-project/botho/issues/925); shares the horizon with [#833](https://github.com/botho-project/botho/issues/833) / [#831](https://github.com/botho-project/botho/issues/831))
**Status:** Simulation + read-out. This is a calibration artifact that informs the #925 ratification; it does **not** ship in the node.
**Reproduce:**
```
cargo run -p bth-cluster-tax --features cli --bin cluster-tax-sim -- honest-overcharge-sweep
```
Sim source: `cluster-tax/src/simulation/downgrade_overcharge_sweep.rs` (deterministic; seed `0xB07A_0925`). Every number below is regenerated by that command — nothing is hand-computed.

---

## 1. What this measures (and why it is new)

Issue #925 closes the demurrage class-transition leak proven by the consensus test
`demurrage_background_reset_leak_is_real` (verdict
[`demurrage-background-reset-leak.md`](demurrage-background-reset-leak.md), #834): a wealthy
holder can spend a *young* wealthy coin to a fully background-tagged output, pay ≈0 accrued
demurrage, and escape **all** future stock-level demurrage. The fix prices that transition by
charging **capitalized future demurrage** over the shared `SETTLEMENT_HORIZON_BLOCKS` on any
spend whose declared output cluster mass drops **below the ring-implied input cluster floor** —
the same primitive #831/settlement uses (see
[`settlement-horizon-calibration.md`](settlement-horizon-calibration.md)).

The settlement sweep already proved the churn-invariance margin is `1.0000` at every horizon
(settling is never cheaper than holding). But it never had to measure a risk **unique to #925**:
unlike #831's *opt-in* settlement op, #925 auto-applies to **every** deflating spend. That raises
the possibility of **over-charging honest activity** that merely looks like a downgrade. This
sweep quantifies that false-positive surface over a realistic honest agent mix, and confirms the
keying still charges the exploit.

## 2. The keying under test

For a spend of `value` picocredits, reusing the shipped consensus kernels verbatim:

```text
input_floor   = ring_centroid_implied_factor(ring)          // value-weighted, decoys included
output_factor = curve.factor(declared_output_cluster_wealth)
downgrade_charge = if output_factor < input_floor {
    demurrage_charge(value, input_floor,   H) − demurrage_charge(value, output_factor, H)
} else { 0 }                                                 // in-class / inflation ⇒ no charge
```

The subtraction is the **net** capitalized future demurrage escaped by dropping from the floor
class to the declared class over horizon `H`. Two structural facts fall straight out of the
kernels (`cluster-tax/src/demurrage.rs`, `cluster-tax/src/fee_curve.rs`):

- **A genuine background→background spend floors at 1×.** A background spender's real input and
  honest decoys all carry zero cluster wealth, so the value-weighted centroid is 0 and
  `curve.factor(0) == 1000` (exactly 1×). The declared output is also 1×, so
  `output_factor < input_floor` is `1000 < 1000` = false ⇒ **zero charge**. This is the #950
  acceptance case.
- **An honest in-class wealthy spend never downgrades.** The spender declares its true (full)
  class; honest decoys can only *dilute* the value-weighted floor **downward**, so the declared
  output is always `≥` the floor ⇒ never `<` ⇒ zero charge.

The **only** way an honest spend can trip a non-zero charge is if a background spender declares a
1× output while the ring floor is lifted **above** 1× — and the sole channel for that is a
**wealthy decoy** contaminating a background spender's ring. Because the floor is value-weighted,
a single high-value wealthy decoy dominates the centroid and inflates the floor (the reverse of
the H2 age-dilution the quantile kernel guards against). §4.5 isolates and sizes that channel.

## 3. Method

A focused, deterministic Monte-Carlo — same shape as the sibling `settlement_horizon_sweep` and
the shipped `decoy_quantile_sweep`. It reuses the consensus `demurrage_charge` and
`ring_centroid_implied_factor` kernels and the production `ClusterFactorCurve` — no
reimplementation. The honest population is sourced from the shipped `simulation::agents`
archetypes: the spend *sizes* are read off the real `RetailUserAgent`, `MerchantAgent`,
`MixerServiceAgent` and `WhaleAgent` structs by driving their `decide_action`. Each spend gets a
ring (real input + 10 decoys) drawn from the UTXO wealth distribution; the input floor and output
factor come from the kernels; the downgrade charge is evaluated at every horizon. The exploit
spend replicates `demurrage_background_reset_leak_is_real` (10M-BTH cluster, 1000-BTH coin).

## 4. Results

### 4.1 Honest archetypes (agent-sourced)

| archetype | agent | weight | spender class | output class | coin value (BTH) |
|-----------|-------|-------:|-------------:|------------:|-----------------:|
| retail-purchase | RetailUserAgent | 50 | 0 BTH | 0 BTH | 283 |
| merchant-supplier | MerchantAgent | 20 | 0 BTH | 0 BTH | 10000 |
| mixer-withdrawal | MixerServiceAgent | 15 | 0 BTH | 0 BTH | 500 |
| whale-consolidation | WhaleAgent | 6 | 10000000 BTH | 10000000 BTH | 50000 |
| whale-change-making | WhaleAgent | 5 | 10000000 BTH | 10000000 BTH | 10000 |
| whale-inclass-transfer | WhaleAgent | 4 | 10000000 BTH | 10000000 BTH | 8000 |

### 4.2 Downgrade classification per archetype (clean rings)

Input floor = `ring_centroid_implied_factor` over the real input + honest (background) decoys. A
downgrade is charged only when the declared output factor is strictly below the floor. Honest
rows never downgrade; the exploit does.

| archetype | input floor | output factor | downgrade? | charge @1yr | charge @5yr |
|-----------|-----------:|-------------:|:---------:|-----------:|-----------:|
| retail-purchase | 1.000x | 1.000x | no | 0.0000% | 0.0000% |
| merchant-supplier | 1.000x | 1.000x | no | 0.0000% | 0.0000% |
| mixer-withdrawal | 1.000x | 1.000x | no | 0.0000% | 0.0000% |
| whale-consolidation | 5.697x | 5.745x | no | 0.0000% | 0.0000% |
| whale-change-making | 5.632x | 5.745x | no | 0.0000% | 0.0000% |
| whale-inclass-transfer | 5.585x | 5.745x | no | 0.0000% | 0.0000% |
| **exploit (young-wealthy→bg)** | 5.745x | 1.000x | **YES** | **1.8980%** | **9.4900%** |

**Reading it:** every honest archetype classifies as **not a downgrade** and pays **0.0000%** at
both the shortest and longest horizon. The three background patterns floor at exactly 1× (output
== floor). The three whale patterns declare the full 5.745× class while honest decoys dilute the
floor slightly *below* that (5.585×–5.697×) — so the declared output is *above* the floor and
there is nothing to price. Only the exploit declares below its floor, and it is charged.

### 4.3 Honest false-positive rate & over-charge (clean rings, realistic case)

Monte-Carlo over 20000 honest spends (seed `0xB07A0925`), ring size 11, all-background decoys
(0% contamination). False-positive rate = fraction of honest spends charged a non-zero downgrade
fee.

**Honest false-positive rate: 0.0000% (0 / 20000 spends).**

| horizon | honest FP rate | mean over-charge (% of value) | max over-charge |
|---------|--------------:|------------------------------:|----------------:|
| 1mo (0.08yr) | 0.0000% | 0.0000% | 0.0000% |
| 6mo (0.50yr) | 0.0000% | 0.0000% | 0.0000% |
| 1yr (1.00yr) | 0.0000% | 0.0000% | 0.0000% |
| 2yr (2.00yr) | 0.0000% | 0.0000% | 0.0000% |
| 5yr (5.00yr) | 0.0000% | 0.0000% | 0.0000% |

**Reading it:** over a realistic honest mix, **not one** of 20,000 honest spends is over-charged,
**at any horizon**. The keying is tight: honest activity stays at exactly zero downgrade charge.

### 4.4 Exploit downgrade charge by horizon (the leak is priced)

Young-wealthy→background spend: 10000000-BTH cluster (5.745× floor), 1000-BTH coin, declared
background (1.000× output). Charge = capitalized future demurrage; multiple = charge ÷ one year
of ordinary demurrage at the floor (= horizon in years, by linearity).

| horizon | exploit charge (% of value) | × annual demurrage |
|---------|----------------------------:|-------------------:|
| 1mo (0.08yr) | 0.1582% | 0.08× |
| 6mo (0.50yr) | 0.9490% | 0.50× |
| 1yr (1.00yr) | 1.8980% | 1.00× |
| 2yr (2.00yr) | 3.7960% | 2.00× |
| 5yr (5.00yr) | 9.4900% | 5.00× |

**Reading it:** the exploit that pays **zero** today is charged a real, monotone-in-horizon fee —
1.90% of the coin at 1yr, 9.49% at 5yr. The `1.00×` at 1yr confirms the cross-check against
`demurrage_background_reset_leak_is_real`: the charge equals exactly the honest annual demurrage
the wealthy coin would have owed (18.98 BTH on a 1000-BTH coin), now capitalized over the
horizon. The leak is closed at every horizon.

### 4.5 Sensitivity: honest FP rate & over-charge vs decoy contamination

The **only** channel to an honest over-charge: a wealthy decoy contaminating a background
spender's value-weighted ring floor. FP rate is horizon-independent (classification is
`output < floor`); the over-charge magnitude below is at the 5yr horizon (worst case).
Contamination = fraction of decoys that are wealthy coins.

| contamination | honest FP rate | mean over-charge @5yr | max over-charge @5yr |
|--------------:|--------------:|----------------------:|---------------------:|
| 0.0% | 0.0000% | 0.0000% | 0.0000% |
| 1.0% | 8.2800% | 9.3879% | 9.4820% |
| 5.0% | 34.0650% | 9.3973% | 9.4860% |
| 10.0% | 55.3500% | 9.4131% | 9.4880% |

**Reading it:** contamination is a knife-edge. At **0%** (class-homogeneous rings, the realistic
case) there are zero false positives. But once wealthy coins leak into background spenders' rings,
the false-positive rate climbs fast — 8.3% at 1% contamination, 55% at 10% — because a single
high-value wealthy decoy is enough to dominate the value-weighted centroid and lift a background
spender's floor to (nearly) the full wealthy class. And when it trips, it trips **hard**: the
mean over-charge is ~9.39% of the small coin's value at 5yr, essentially the full whale-class
capitalized demurrage applied to an innocent commerce coin. This is the reverse of the H2
age-dilution attack — here value-weighting *inflates* the floor instead of a spender diluting it.

### 4.6 Factor-similarity decoy band — closing the §4.5 channel (issue #925 Part 1)

§4.5 leaves exactly one wallet-layer lever: a background spender must not draw a high-value
wealthy coin as a decoy. This is the direct analog of the age-dilution concern PR #595 closed
for the elapsed term with the **`AGE_SIMILARITY_SPREAD_BPS = 1000` (±10%)** age band. We
close the factor channel the same way: a background (1×) spender draws only decoys whose
cluster factor sits within a band around its real input's factor. The sweep below applies that
filter at the **pathological 5% raw contamination** (the §4.5 level that produced a 34.1% FP
rate *unfiltered*) and finds the widest band that still keeps the honest false-positive rate at
0%, while retaining the same-class decoy pool. Reproduce with `honest-overcharge-sweep`; every
number is regenerated, nothing hand-computed.

| factor band | band edge (×) | wealthy classes admitted | eligible pool | same-class pool | honest FP rate | mean over-charge @5yr | FP-safe |
|------------:|--------------:|-------------------------:|--------------:|----------------:|---------------:|----------------------:|:-------:|
| ±5% | 1.050× | 0 / 6 | 95.00% | 100.00% | 0.0000% | 0.0000% | yes |
| ±10% | 1.100× | 0 / 6 | 95.00% | 100.00% | 0.0000% | 0.0000% | yes |
| ±20% | 1.200× | 0 / 6 | 95.00% | 100.00% | 0.0000% | 0.0000% | yes |
| ±35% | 1.350× | 0 / 6 | 95.00% | 100.00% | 0.0000% | 0.0000% | yes |
| ±50% | 1.500× | 0 / 6 | 95.00% | 100.00% | 0.0000% | 0.0000% | yes |
| ±90% | 1.900× | 0 / 6 | 95.00% | 100.00% | 0.0000% | 0.0000% | yes |
| ±100% | 2.000× | 1 / 6 | 95.83% | 100.00% | 8.0300% | 1.5649% | NO |
| ±250% | 3.500× | 3 / 6 | 97.50% | 100.00% | 22.3000% | 3.3979% | NO |
| ±500% | 6.000× | 6 / 6 | 100.00% | 100.00% | 41.6500% | 6.1196% | NO |

**Reading it:** because the factor curve is flat at *exactly* 1× for background wealth, the
same-class (background) decoy pool is retained at **100% at every band width** — unlike the
continuous age band, tightening the factor band never costs a background spender any same-class
candidate. The band's only job is to keep its **upper** edge below the nearest wealthy class.
The nearest wealthy class is 1.939× (a 10k-BTH cluster), which enters the band only at ±100%
(edge 2.000×). Every band up to **±90%** therefore drives the honest FP rate to **exactly 0%**;
at ±100% the first wealthy class bleeds in and FP jumps to 8.03%, then rises monotonically as
wider bands admit more classes.

**Recommended `FACTOR_SIMILARITY_SPREAD` = ±10% (1000 bps)** — the established
`AGE_SIMILARITY_SPREAD_BPS` convention, sitting ~9× inside the widest FP-safe band (±90%). It
gives a background spender the full same-class background pool (maximal within-class anonymity)
with a large margin against cross-class contamination, and by construction caps any admitted
decoy's factor at 1.100× — so even a worst-case in-band decoy could lift the floor by at most
10% of the 1× minimum, exactly mirroring the ≤1.10 over-charge bound the age band guarantees.
Part 3 wires this constant into `botho/src/decoy_selection.rs`.

## 5. Read-out: is the keying tight enough?

**Yes, at the consensus layer — conditional on decoy-selection hygiene at the wallet layer.**

- **Honest activity is not over-charged.** Under class-homogeneous rings (the realistic default),
  the honest false-positive rate is **0.0000% at every horizon**, and the exploit is charged
  1.90%–9.49% depending on horizon. Honest background→background floors at exactly 1× and pays
  zero; honest in-class whale spends declare above their diluted floor and pay zero. The `output <
  floor` keying cleanly separates the exploit from honest deflation-free spends. The horizon
  choice does **not** trade off against honest over-charge — the honest side is zero at 1mo and at
  5yr alike — so this sweep imposes no new constraint on the shared `SETTLEMENT_HORIZON_BLOCKS`
  recommended in [`settlement-horizon-calibration.md`](settlement-horizon-calibration.md) (5yr,
  with 2yr as the accessibility-leaning alternative).

- **The one residual risk is a wallet-layer decoy-selection concern, not a consensus flaw.** The
  value-weighted ring floor that catches the exploit is the *same* mechanism that, if a background
  spender's ring is contaminated by a high-value wealthy decoy, inflates that spender's floor and
  over-charges them the near-full wealthy-class fee (§4.5). This cannot be tightened by moving the
  horizon. Two levers, in preference order:
  1. **Decoy-selection hygiene (recommended).** Background spends should not draw high-value
     wealthy coins as decoys. Age-biased selection already makes wealthy coins a small minority of
     the eligible set, so real-world contamination sits near the 0% end of §4.5 — but the tail is
     costly enough (9.4% of value per trip) that wallets should explicitly value-bound or
     class-bound decoys for background spends. This keeps the anti-gaming floor intact.
  2. **Charge on the deflation gap, not the raw floor (optional consensus refinement).** If the
     charge keyed off the *declared input* class the spender is deflating *from* (the inherited
     tags), rather than the value-weighted whole-ring centroid, a background spender with a
     wealthy decoy would not be charged — but this weakens the very floor that catches the exploit
     (the exploiter would then simply under-declare their input). Lever 1 is the safer fix; lever 2
     should only be considered under its own ADR with its own evidence.

**Bottom line for #925 ratification:** the auto-applied downgrade charge does **not** over-charge
honest activity at any horizon (0% false positives on realistic rings), and it prices the #834
exploit at 1.90%–9.49% of value. The keying is sound. The only caveat to flag in the ADR is that
wallets must not put wealthy coins in background spenders' decoy rings — a selection-policy note,
not a blocker.

## 6. What this deliverable is (and isn't)

This is a **calibration / correctness artifact** for the #925 ratification, mirroring the
settlement-horizon sweep. It confirms the honest-overcharge surface is empty on realistic rings
and that the exploit stays priced; it does not itself wire anything into consensus. The sim is
committed and seeded so the numbers regenerate on demand and can be re-run against different
population assumptions (agent mix, ring size, decoy value ranges, contamination) if the maintainer
wants to probe the recommendation further.
